/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import * as nestAccessControl from "nest-access-control";
import * as defaultAuthGuard from "../../auth/defaultAuth.guard";
import { WeatherDatumService } from "../weatherDatum.service";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { WeatherDatumCreateInput } from "./WeatherDatumCreateInput";
import { WeatherDatum } from "./WeatherDatum";
import { Post } from "../../post/base/Post";
import { WeatherDatumFindManyArgs } from "./WeatherDatumFindManyArgs";
import { WeatherDatumWhereUniqueInput } from "./WeatherDatumWhereUniqueInput";
import { WeatherDatumUpdateInput } from "./WeatherDatumUpdateInput";

@swagger.ApiBearerAuth()
@common.UseGuards(defaultAuthGuard.DefaultAuthGuard, nestAccessControl.ACGuard)
export class WeatherDatumControllerBase {
  constructor(
    protected readonly service: WeatherDatumService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}
  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Post()
  @swagger.ApiCreatedResponse({ type: WeatherDatum })
  @swagger.ApiBody({
    type: WeatherDatumCreateInput,
  })
  @nestAccessControl.UseRoles({
    resource: "WeatherDatum",
    action: "create",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async createWeatherDatum(
    @common.Body() data: WeatherDatumCreateInput
  ): Promise<WeatherDatum> {
    return await this.service.createWeatherDatum({
      data: {
        ...data,

        posts: data.posts
          ? {
              connect: data.posts,
            }
          : undefined,
      },
      select: {
        createdAt: true,
        currentWeather: true,
        id: true,

        posts: {
          select: {
            id: true,
          },
        },

        updatedAt: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get()
  @swagger.ApiOkResponse({ type: [WeatherDatum] })
  @ApiNestedQuery(WeatherDatumFindManyArgs)
  @nestAccessControl.UseRoles({
    resource: "WeatherDatum",
    action: "read",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async weatherData(@common.Req() request: Request): Promise<WeatherDatum[]> {
    const args = plainToClass(WeatherDatumFindManyArgs, request.query);
    return this.service.weatherData({
      ...args,
      select: {
        createdAt: true,
        currentWeather: true,
        id: true,

        posts: {
          select: {
            id: true,
          },
        },

        updatedAt: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: WeatherDatum })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "WeatherDatum",
    action: "read",
    possession: "own",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async weatherDatum(
    @common.Param() params: WeatherDatumWhereUniqueInput
  ): Promise<WeatherDatum | null> {
    const result = await this.service.weatherDatum({
      where: params,
      select: {
        createdAt: true,
        currentWeather: true,
        id: true,

        posts: {
          select: {
            id: true,
          },
        },

        updatedAt: true,
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: WeatherDatum })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @swagger.ApiBody({
    type: WeatherDatumUpdateInput,
  })
  @nestAccessControl.UseRoles({
    resource: "WeatherDatum",
    action: "update",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async updateWeatherDatum(
    @common.Param() params: WeatherDatumWhereUniqueInput,
    @common.Body() data: WeatherDatumUpdateInput
  ): Promise<WeatherDatum | null> {
    try {
      return await this.service.updateWeatherDatum({
        where: params,
        data: {
          ...data,

          posts: data.posts
            ? {
                connect: data.posts,
              }
            : undefined,
        },
        select: {
          createdAt: true,
          currentWeather: true,
          id: true,

          posts: {
            select: {
              id: true,
            },
          },

          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: WeatherDatum })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "WeatherDatum",
    action: "delete",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async deleteWeatherDatum(
    @common.Param() params: WeatherDatumWhereUniqueInput
  ): Promise<WeatherDatum | null> {
    try {
      return await this.service.deleteWeatherDatum({
        where: params,
        select: {
          createdAt: true,
          currentWeather: true,
          id: true,

          posts: {
            select: {
              id: true,
            },
          },

          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }
}
